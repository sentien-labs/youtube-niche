# Copy to .env and fill in. The tool loads .env automatically (and never overrides
# variables already exported in your shell).

# --- YouTube auth: provide EITHER an API key OR OAuth files (API key wins if both set) ---

# Option 1: a YouTube Data API v3 key from a Google Cloud project. Simplest.
YOUTUBE_API_KEY=your-youtube-data-api-v3-key

# Option 2: OAuth credentials (used automatically when YOUTUBE_API_KEY is unset and both
# files exist). Use absolute paths on your machine.
# YOUTUBE_OAUTH_CLIENT_SECRET=/absolute/path/to/client_secret.json
# YOUTUBE_OAUTH_TOKEN=/absolute/path/to/token.json

# LLM signals (comment-demand mining + content-depth scoring). CLI providers use their
# own subscription login; anthropic uses ANTHROPIC_API_KEY.
# LLM_PROVIDER=codex  # auto | anthropic | codex | claude | agy | grok
# Note: LLM_PROVIDER=grok is a Grok CLI reasoning backend, not native X/Twitter data access.
# GROK_MODEL=grok-composer-2.5-fast  # optional; try grok-build for deeper Grok runs
# GROK_COMMENT_MODEL=grok-composer-2.5-fast  # optional tier override for cheap/comment calls
# GROK_QUALITY_MODEL=grok-build  # optional tier override for quality/niche extraction calls
# ANTHROPIC_API_KEY=your-anthropic-key
# LLM_FALLBACK=0  # disables the automatic LLM provider failover chain (default: on)

# Optional fallback for Google Trends (signal F momentum + 5-year durability) when pytrends
# fails — pytrends is unofficial and its upstream repo is dead (archived April 2025, chronic
# 429s). SerpApi's google_trends engine is used only if pytrends errors or returns no data.
# Free tier: 250 searches/month at serpapi.com.
# SERPAPI_KEY=your-serpapi-key

# Optional measurement defaults
# YOUTUBE_REGION_CODE=US
# YOUTUBE_RELEVANCE_LANGUAGE=en
# TRENDS_GEO=US
# YOUTUBE_DAILY_SEARCH_LIMIT=100
# QUERY_SAMPLES=1  # set to 3 for stricter multi-query niche validation
# KEYWORD_METRICS_CSV=/absolute/path/to/keyword-metrics.csv
# CACHE_ONLY=false  # true = only reuse cached YouTube API responses; never spend quota
